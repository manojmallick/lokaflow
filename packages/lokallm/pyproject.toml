[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lokallm"
version = "0.1.0"
description = "LokaFlow™ Local Fine-Tuning Pipeline for Phi-3"
readme = "README.md"
requires-python = ">=3.10"
license = "BUSL-1.1"
authors = [
    { name = "LearnHubPlay BV" }
]
dependencies = [
    "torch>=2.2.0",
    "transformers>=4.40.0",
    "peft>=0.10.0",
    "trl>=0.8.1",
    "datasets>=2.18.0",
    "bitsandbytes>=0.43.0",
    "accelerate>=0.29.0",
    "requests>=2.31.0",
    "tqdm>=4.66.0"
]

[project.scripts]
loka-generate = "lokallm.data.generate:main"
loka-train = "lokallm.train.lora:main"
loka-export = "lokallm.export.gguf:main"
